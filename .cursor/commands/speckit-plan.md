# Speckit: plan

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-plan** workflow using the instructions in `.agents/skills/speckit-plan/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-plan.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
