# Speckit: onboard → start

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-start** workflow using the instructions in `.agents/skills/speckit-onboard-start/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-start.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
