# Speckit: ralph → run

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-ralph-run** workflow using the instructions in `.agents/skills/speckit-ralph-run/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-ralph-run.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
