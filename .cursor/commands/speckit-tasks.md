# Speckit: tasks

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-tasks** workflow using the instructions in `.agents/skills/speckit-tasks/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-tasks.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
