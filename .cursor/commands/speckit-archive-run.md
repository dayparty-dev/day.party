# Speckit: archive → run

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-archive-run** workflow using the instructions in `.agents/skills/speckit-archive-run/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-archive-run.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
