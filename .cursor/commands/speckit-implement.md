# Speckit: implement

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-implement** workflow using the instructions in `.agents/skills/speckit-implement/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-implement.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
