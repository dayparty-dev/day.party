# Speckit: onboard → explain

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-explain** workflow using the instructions in `.agents/skills/speckit-onboard-explain/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-explain.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
