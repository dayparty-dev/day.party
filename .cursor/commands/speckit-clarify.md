# Speckit: clarify

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-clarify** workflow using the instructions in `.agents/skills/speckit-clarify/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-clarify.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
