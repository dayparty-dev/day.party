# Speckit: onboard → mentor

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-mentor** workflow using the instructions in `.agents/skills/speckit-onboard-mentor/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-mentor.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
