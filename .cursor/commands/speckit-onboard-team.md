# Speckit: onboard → team

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-team** workflow using the instructions in `.agents/skills/speckit-onboard-team/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-team.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
