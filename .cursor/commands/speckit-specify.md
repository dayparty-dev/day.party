# Speckit: specify

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-specify** workflow using the instructions in `.agents/skills/speckit-specify/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-specify.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
