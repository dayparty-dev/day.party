# Speckit: product-forge → revalidate

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-revalidate** workflow using the instructions in `.agents/skills/speckit-product-forge-revalidate/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-revalidate.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
