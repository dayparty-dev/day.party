# Speckit: onboard → trail

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-trail** workflow using the instructions in `.agents/skills/speckit-onboard-trail/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-trail.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
