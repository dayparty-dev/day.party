# Speckit: maqa-github-projects → populate

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-maqa-github-projects-populate** workflow using the instructions in `.agents/skills/speckit-maqa-github-projects-populate/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-maqa-github-projects-populate.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
