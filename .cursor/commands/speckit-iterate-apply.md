# Speckit: iterate → apply

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-iterate-apply** workflow using the instructions in `.agents/skills/speckit-iterate-apply/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-iterate-apply.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
