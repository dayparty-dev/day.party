# Speckit: product-forge → status

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-status** workflow using the instructions in `.agents/skills/speckit-product-forge-status/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-status.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
