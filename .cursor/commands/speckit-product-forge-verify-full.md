# Speckit: product-forge → verify-full

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-verify-full** workflow using the instructions in `.agents/skills/speckit-product-forge-verify-full/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-verify-full.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
