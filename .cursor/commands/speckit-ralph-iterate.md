# Speckit: ralph → iterate

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-ralph-iterate** workflow using the instructions in `.agents/skills/speckit-ralph-iterate/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-ralph-iterate.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
