# Speckit: learn → clarify

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-learn-clarify** workflow using the instructions in `.agents/skills/speckit-learn-clarify/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-learn-clarify.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
