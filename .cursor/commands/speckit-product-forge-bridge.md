# Speckit: product-forge → bridge

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-bridge** workflow using the instructions in `.agents/skills/speckit-product-forge-bridge/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-bridge.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
