# Speckit: product-forge → test-plan

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-test-plan** workflow using the instructions in `.agents/skills/speckit-product-forge-test-plan/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-test-plan.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
