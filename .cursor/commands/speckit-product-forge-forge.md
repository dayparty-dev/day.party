# Speckit: product-forge → forge

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-forge** workflow using the instructions in `.agents/skills/speckit-product-forge-forge/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-forge.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
