# Speckit: learn → review

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-learn-review** workflow using the instructions in `.agents/skills/speckit-learn-review/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-learn-review.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
