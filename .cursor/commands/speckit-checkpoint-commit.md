# Speckit: checkpoint → commit

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-checkpoint-commit** workflow using the instructions in `.agents/skills/speckit-checkpoint-commit/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-checkpoint-commit.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
