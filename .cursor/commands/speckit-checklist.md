# Speckit: checklist

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-checklist** workflow using the instructions in `.agents/skills/speckit-checklist/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-checklist.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
