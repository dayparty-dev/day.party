# Speckit: retrospective → analyze

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-retrospective-analyze** workflow using the instructions in `.agents/skills/speckit-retrospective-analyze/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-retrospective-analyze.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
