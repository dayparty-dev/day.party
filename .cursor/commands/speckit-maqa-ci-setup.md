# Speckit: maqa-ci → setup

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-maqa-ci-setup** workflow using the instructions in `.agents/skills/speckit-maqa-ci-setup/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-maqa-ci-setup.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
