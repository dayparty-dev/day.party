# Speckit: analyze

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-analyze** workflow using the instructions in `.agents/skills/speckit-analyze/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-analyze.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
