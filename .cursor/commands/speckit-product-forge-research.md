# Speckit: product-forge → research

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-research** workflow using the instructions in `.agents/skills/speckit-product-forge-research/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-research.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
