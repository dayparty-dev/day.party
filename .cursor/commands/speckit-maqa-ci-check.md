# Speckit: maqa-ci → check

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-maqa-ci-check** workflow using the instructions in `.agents/skills/speckit-maqa-ci-check/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-maqa-ci-check.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
