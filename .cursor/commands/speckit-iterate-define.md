# Speckit: iterate → define

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-iterate-define** workflow using the instructions in `.agents/skills/speckit-iterate-define/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-iterate-define.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
