# Speckit: product-forge → test-run

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-test-run** workflow using the instructions in `.agents/skills/speckit-product-forge-test-run/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-test-run.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
