# Speckit: product-forge → implement

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-implement** workflow using the instructions in `.agents/skills/speckit-product-forge-implement/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-implement.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
