# Speckit: maqa-github-projects → setup

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-maqa-github-projects-setup** workflow using the instructions in `.agents/skills/speckit-maqa-github-projects-setup/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-maqa-github-projects-setup.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
