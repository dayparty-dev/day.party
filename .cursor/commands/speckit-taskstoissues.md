# Speckit: taskstoissues

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-taskstoissues** workflow using the instructions in `.agents/skills/speckit-taskstoissues/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-taskstoissues.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
