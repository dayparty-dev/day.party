# Speckit: onboard → badge

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-badge** workflow using the instructions in `.agents/skills/speckit-onboard-badge/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-badge.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
