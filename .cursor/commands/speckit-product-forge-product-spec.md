# Speckit: product-forge → product-spec

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-product-forge-product-spec** workflow using the instructions in `.agents/skills/speckit-product-forge-product-spec/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-product-forge-product-spec.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
