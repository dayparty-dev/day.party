# Speckit: constitution

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-constitution** workflow using the instructions in `.agents/skills/speckit-constitution/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-constitution.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
