# Speckit: onboard → quiz

User input (feature / context):

```text
$ARGUMENTS
```

Execute the **speckit-onboard-quiz** workflow using the instructions in `.agents/skills/speckit-onboard-quiz/SKILL.md` (portable Agent Skills layout) or `.cursor/agents/speckit-onboard-quiz.md` (Cursor subagent). Use the fenced block above as the workflow’s user input / `$ARGUMENTS`.
